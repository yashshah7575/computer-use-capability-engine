# AI-DLC operational metadata

This directory is **tool-required**. The workflow reads and writes `aidlc-state.md`, `audit.md`, and stage files under `inception/`.

Reviewer-facing copies of requirements and decisions are in [`/ai-dlc`](../ai-dlc/README.md).
