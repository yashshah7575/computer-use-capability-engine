# Design

The assignment-required design write-up is **[REPORT.md](../../REPORT.md)** at the repository root (do not relocate it).

It covers:

1. Architecture
2. Artifact schema
3. Determinism and error handling
4. Heterogeneity and multi-tenant
5. Escalation and handoff
6. Safety
7. Cuts

Capability JSON consumed at runtime lives in `/artifacts` (for example `lookup-savings-balance.json`), not in this folder.
